services:
  - type: web
    name: onpoint-socketio
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: CLERK_SECRET_KEY
        sync: false
      - key: UPSTASH_REDIS_URL
        sync: false
      - key: ALLOWED_ORIGIN
        sync: false
